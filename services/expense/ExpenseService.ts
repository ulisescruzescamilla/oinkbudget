import { ExpenseType } from "@/types/ExpenseType"

export const saveExpense = (expense: ExpenseType ) => {
  console.debug('saving expense',expense)
  // save expense on expenses table
  // update budget
  // update wallets / accounts
}